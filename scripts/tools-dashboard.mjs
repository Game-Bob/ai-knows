export function createDashboardHtml() {
    const html = `<!doctype html>
<html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#090d19"><title>Tools Pulse</title>
<style>
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#e7ebf5;background:#090d19;color-scheme:dark;--panel:#11182a;--panel2:#0d1425;--line:#25304a;--muted:#8995ad;--purple:#9a8cff;--green:#52d6a4;--red:#ff7182;--yellow:#f6c85f;--cyan:#60c7f7}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 85% -10%,#283568 0,#090d19 36%);min-height:100vh;overflow-x:hidden}.app{display:grid;grid-template-columns:225px minmax(0,1fr);min-height:100vh}.sidebar{border-right:1px solid var(--line);padding:24px 14px;background:#0b1020e8;position:sticky;top:0;height:100vh}.brand{display:flex;align-items:center;gap:10px;margin:0 8px 34px}.brand-mark{display:grid;place-items:center;width:34px;height:34px;background:linear-gradient(135deg,#9a8cff,#60c7f7);color:#0b1020;border-radius:11px;font-weight:950}.brand strong{font-size:15px}.brand small{display:block;color:var(--muted);font-size:10px}.nav-label{color:#68758e;font-size:10px;text-transform:uppercase;font-weight:800;letter-spacing:.14em;margin:0 10px 8px}.nav-item{color:#aab4c9;padding:10px 11px;border-radius:9px;margin:3px 0;font-size:12px;cursor:pointer}.nav-item.active{background:#8f83ff1a;color:#fff}.nav-item:hover{background:#ffffff0b}.sidebar-foot{position:absolute;left:22px;right:22px;bottom:20px;color:#68758e;font-size:10px;line-height:1.8}.main{padding:28px clamp(18px,3.8vw,52px) 54px;max-width:1900px;width:100%;min-width:0}.topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:20px;margin-bottom:18px}.kicker{font-size:10px;color:var(--purple);font-weight:850;text-transform:uppercase;letter-spacing:.17em}.topbar h1{font-size:clamp(25px,3vw,39px);letter-spacing:-.055em;line-height:1.04;margin:7px 0 0}.actions{display:flex;gap:8px;align-items:center}.button,.select{font:inherit;color:#eef1fb;border:1px solid var(--line);border-radius:9px;background:#151e33;padding:9px 11px;cursor:pointer;font-size:12px}.button:hover,.select:hover{border-color:#6c75a7;background:#1a2540}.button.primary{border:0;background:linear-gradient(135deg,#8176f5,#5c98f3);font-weight:800}.status-live{display:flex;align-items:center;gap:7px;color:var(--muted);font-size:11px}.status-live i{width:7px;height:7px;border-radius:50%;background:var(--green);box-shadow:0 0 13px var(--green)}.source-picker,.metric-picker,.granularity-picker{display:flex;gap:4px;padding:4px;background:#0a1020;border:1px solid var(--line);border-radius:11px;width:max-content;max-width:100%}.source-picker{margin-bottom:12px}.source-picker button,.metric-picker button,.granularity-picker button{border:0;background:transparent;color:var(--muted);font:inherit;font-size:11px;font-weight:800;padding:8px 12px;border-radius:7px;cursor:pointer}.source-picker button.active,.metric-picker button.active,.granularity-picker button.active{background:linear-gradient(135deg,#8176f5,#5c98f3);color:#fff}.source-picker button:hover,.metric-picker button:hover,.granularity-picker button:hover{color:#fff}.source-picker small{display:block;font-size:9px;font-weight:500;opacity:.7;margin-top:1px}.filters{display:grid;grid-template-columns:1.7fr repeat(4,1fr) auto;gap:8px;align-items:end;margin-bottom:14px}.filter{display:block;color:var(--muted);font-weight:700;font-size:10px}.filter input,.filter select{display:block;width:100%;margin-top:5px;border:1px solid var(--line);border-radius:8px;background:#0d1425;color:#edf0f9;padding:9px;font:inherit;font-size:11px;min-width:0}.filter input:focus,.filter select:focus{outline:2px solid #8e83ff55;border-color:var(--purple)}.clear{color:#aab4c9;background:transparent;border:0;cursor:pointer;font-size:11px;padding:9px 4px}.cards{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin-bottom:14px}.card,.panel{border:1px solid var(--line);background:linear-gradient(145deg,#141c30e8,#0f1627e8);border-radius:14px;box-shadow:0 16px 40px #0000001f}.card{padding:15px 16px;min-height:96px}.card-label{color:var(--muted);font-size:10px;display:flex;justify-content:space-between;gap:6px}.card strong{display:block;font-size:25px;letter-spacing:-.045em;margin:9px 0 3px;font-variant-numeric:tabular-nums}.card-note{font-size:10px;color:var(--muted)}.up{color:var(--green)!important}.down{color:var(--red)!important}.yellow{color:var(--yellow)!important}.layout{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(300px,.85fr);gap:14px}.panel{padding:17px;margin-top:14px;min-width:0}.panel-title{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.panel h2{font-size:15px;margin:0 0 3px;letter-spacing:-.02em}.panel-subtitle{font-size:11px;color:var(--muted);margin:0}.chart{height:280px;margin-top:15px;position:relative}.chart svg{width:100%;height:100%;overflow:visible}.chart text{font:10px Inter,system-ui,sans-serif;fill:#76839b}.chart .grid{stroke:#26304a;stroke-width:1}.chart .line{fill:none;stroke:var(--purple);stroke-width:3;stroke-linejoin:round;stroke-linecap:round}.chart .point{fill:#11182a;stroke:var(--purple);stroke-width:2.5}.empty{display:grid;place-items:center;min-height:160px;color:var(--muted);border:1px dashed #34415e;border-radius:10px}.list{display:flex;flex-direction:column;gap:8px;margin-top:15px}.list-row{display:grid;grid-template-columns:23px minmax(0,1fr) auto;gap:8px;align-items:center}.rank{color:#5e6c87;font-size:11px;text-align:center}.list-name{font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.list-name small{display:block;color:#697791;font-size:9px;margin-top:2px}.bar-wrap{height:5px;background:#202b43;border-radius:99px;margin-top:5px;overflow:hidden}.bar{height:100%;border-radius:99px;background:linear-gradient(90deg,var(--purple),var(--cyan));min-width:2px}.list-value{font-size:11px;font-weight:800;font-variant-numeric:tabular-nums}.changes{display:grid;grid-template-columns:1fr 1fr;gap:13px}.change-col h3{font-size:10px;text-transform:uppercase;letter-spacing:.1em;margin:0 0 7px;color:var(--muted)}.change-row{display:flex;justify-content:space-between;gap:8px;padding:8px 0;border-bottom:1px solid #202a41;font-size:11px}.change-row span:first-child{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.change-row span:last-child{font-weight:800;font-variant-numeric:tabular-nums}.table-tools{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-top:14px;color:var(--muted);font-size:10px}.table-wrap{overflow:auto;margin:0 -17px;padding:0 17px;max-height:650px}table{border-collapse:collapse;width:100%;min-width:850px}th,td{padding:9px 8px;border-bottom:1px solid #202a41;white-space:nowrap;text-align:left}th{position:sticky;top:0;background:#141c30;color:#73819c;text-transform:uppercase;font-size:9px;letter-spacing:.08em;cursor:pointer;z-index:1}th:hover{color:#fff}td{font-size:11px;color:#c8cfdd}td small{display:block;color:#697791;font-size:9px;margin-top:2px}.num{text-align:right;font-variant-numeric:tabular-nums}.pill{font-size:9px;font-weight:800;border-radius:99px;padding:4px 7px;display:inline-block}.pill.nueva,.pill.crece{background:#174c4b;color:#76dec1}.pill.decrece{background:#5b2836;color:#ff9aaa}.pill.estable{background:#27334a;color:#aeb9ce}.pill.sin-datos{background:#252d3e;color:#77849b}.pill.sin-comparación{background:#594822;color:#f7d477}.data-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.data-box{border:1px solid var(--line);background:var(--panel2);border-radius:10px;padding:13px}.data-box span{display:block;color:var(--muted);font-size:10px}.data-box strong{display:block;font-size:21px;margin-top:7px;font-variant-numeric:tabular-nums}.footer{color:#65738d;font-size:10px;margin-top:20px}.error{color:#ff9aaa;background:#3d1e2a;border:1px solid #71394b;padding:11px;border-radius:9px;margin-bottom:12px;font-size:11px}.hidden{display:none!important}.page{display:none}.page.active{display:block}@media(max-width:1400px){.cards{grid-template-columns:repeat(3,1fr)}.filters{grid-template-columns:repeat(3,1fr)}.filters label:first-child{grid-column:1/-1}.layout{grid-template-columns:1fr}}@media(max-width:760px){.app{display:block}.sidebar{height:auto;position:relative;border-right:0;border-bottom:1px solid var(--line);padding:16px}.brand{margin-bottom:15px}.nav-label,.nav-item{display:none}.sidebar-foot{display:none}.main{padding:21px 13px 40px}.topbar{display:block}.actions{margin-top:14px;justify-content:space-between}.cards{grid-template-columns:repeat(2,1fr);gap:7px}.card{padding:12px}.card strong{font-size:21px}.filters{display:block}.filter{margin-top:7px}.changes{display:block}.change-col+.change-col{margin-top:17px}.panel{padding:13px}.table-wrap{margin:0 -13px;padding:0 13px}.metric-picker,.granularity-picker{justify-content:flex-start}.data-grid{grid-template-columns:repeat(2,1fr)}}
</style></head><body><div class="app"><aside class="sidebar"><div class="brand"><div class="brand-mark">✦</div><div><strong>Tools Pulse</strong><small>GameBob Studio</small></div></div><div class="nav-label">Vistas</div><div class="nav-item active" data-page="overview">Resumen</div><div class="nav-item" data-page="tools">Tools</div><div class="nav-item" data-page="trend">Tendencia</div><div class="nav-item" data-page="segments">Segmentos</div><div class="nav-item" data-page="alerts">Alertas</div><div class="nav-item" data-page="quality">Calidad</div><div class="sidebar-foot">CSV · JSON<br>actualización automática</div></aside><main class="main"><header class="topbar"><div><div class="kicker" id="source-title">Search Console</div><h1 id="page-title">Resumen</h1></div><div class="actions"><div class="status-live"><i></i><span id="last-updated">—</span></div><button class="button primary" id="refresh">↻</button><button class="button" id="export">↓ CSV</button></div></header><div id="error" class="error hidden"></div><div class="source-picker" aria-label="Fuente"><button data-source="search-console" class="active"><span>Search Console</span><small>SEO</small></button><button data-source="cloudflare"><span>Cloudflare</span><small>Tráfico</small></button></div><section class="filters"><label class="filter">Tool<input id="search" type="search" placeholder="nombre, identidad o slug"></label><label class="filter">Mes<select id="month"><option value="">Último</option></select></label><label class="filter">Dominio<select id="domain"><option value="">Todos</option></select></label><label class="filter">Idioma<select id="language"><option value="">Todos</option></select></label><label class="filter">Categoría<select id="category"><option value="">Todas</option></select></label><button class="clear" id="clear">Limpiar</button></section><section class="page active" id="page-overview"></section><section class="page" id="page-tools"></section><section class="page" id="page-trend"></section><section class="page" id="page-segments"></section><section class="page" id="page-alerts"></section><section class="page" id="page-quality"></section><div class="footer" id="source-info">—</div></main></div><script>
const state={model:null,source:'search-console',metric:'search_clicks',page:'overview',granularity:'month',sort:'value',direction:'desc',filters:{search:'',month:'',domain:'',language:'',category:'',status:''}};const nf=new Intl.NumberFormat('es-ES');const qs=id=>document.getElementById(id);const sourceConfig={'search-console':{title:'Search Console',metrics:['search_clicks','search_impressions']},cloudflare:{title:'Cloudflare',metrics:['pageviews','visits','requests']}};const metricLabels={pageviews:'Pageviews',visits:'Visitas',requests:'Requests',search_clicks:'Clicks GSC',search_impressions:'Impresiones GSC'};const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));const fmt=value=>nf.format(Math.round(Number(value)||0));const pct=value=>value===null||value===undefined?'—':(Number(value)>=0?'+':'')+Number(value).toLocaleString('es-ES',{maximumFractionDigits:1})+'%';const toolKey=row=>row.tool_key||row.tool_id;const val=row=>Number(row[state.metric]||0);const monthBefore=month=>{const date=new Date(month+'-01T00:00:00Z');date.setUTCMonth(date.getUTCMonth()-1);return date.toISOString().slice(0,7)};
function rawFiltered(){const f=state.filters;return (state.model?.rows||[]).filter(row=>(!f.search||[row.tool_name,row.tool_id,row.tool_key,row.slug].some(item=>String(item||'').toLowerCase().includes(f.search)))&&(!f.domain||row.domain===f.domain)&&(!f.language||row.language===f.language)&&(!f.category||row.category===f.category))}function aggregateMonthly(rows){const groups=new Map();for(const row of rows){const key=toolKey(row)+'|'+row.month;let item=groups.get(key);if(!item){item={...row,tool_key:toolKey(row),domains:new Set(),languages:new Set(),categories:new Set()};for(const field of ['pageviews','visits','requests','search_clicks','search_impressions'])item[field]=0}item.domains.add(row.domain);item.languages.add(row.language);item.categories.add(row.category);for(const field of ['pageviews','visits','requests','search_clicks','search_impressions'])item[field]+=Number(row[field]||0);groups.set(key,item)}const out=[...groups.values()].map(row=>({...row,domain:row.domains.size===1?[...row.domains][0]:'global ('+row.domains.size+' dominios)',language:row.languages.size===1?[...row.languages][0]:'global ('+row.languages.size+' idiomas)',category:[...row.categories][0]||row.category,search_ctr:row.search_impressions?row.search_clicks/row.search_impressions*100:0}));const byKey=new Map(out.map(row=>[toolKey(row)+'|'+row.month,row]));const first=new Map();for(const row of out)if(val(row)>0&&(!first.has(toolKey(row))||row.month<first.get(toolKey(row))))first.set(toolKey(row),row.month);return out.map(row=>{const prev=byKey.get(toolKey(row)+'|'+monthBefore(row.month));const current=val(row),previous=prev?val(prev):0;let status='sin datos';if(current>0)status=first.get(toolKey(row))===row.month?'nueva':!prev?'sin comparación':current>previous?'crece':current<previous?'decrece':'estable';return {...row,previousValue:previous,delta:current-previous,changePercent:prev?(previous===0?current>0?null:0:(current-previous)/previous*100):null,status,comparisonMonth:prev?monthBefore(row.month):null})})}function selectedMonth(){return state.filters.month||state.model?.latestMonth||''}function monthlyRows(){const month=selectedMonth();return aggregateMonthly(rawFiltered()).filter(row=>(!month||row.month===month)&&(!state.filters.status||row.status===state.filters.status))}function card(label,value,note='',className=''){return '<article class="card"><div class="card-label">'+label+'</div><strong class="'+className+'">'+value+'</strong><div class="card-note">'+note+'</div></article>'}function panel(title,body,extra=''){return '<section class="panel"><div class="panel-title"><div><h2>'+title+'</h2></div>'+extra+'</div>'+body+'</section>'}
function chart(values,aria){if(!values.length)return '<div class="empty">—</div>';const width=900,height=260,pad={x:45,y:20,bottom:36},max=Math.max(...values.map(item=>item.value),1),x=i=>pad.x+i/Math.max(values.length-1,1)*(width-pad.x-18),y=item=>height-pad.bottom-item.value/max*(height-pad.y-pad.bottom),points=values.map((item,i)=>x(i)+','+y(item)).join(' '),area=pad.x+','+(height-pad.bottom)+' '+points+' '+x(values.length-1)+','+(height-pad.bottom),grid=[0,.5,1].map(r=>{const yy=height-pad.bottom-r*(height-pad.y-pad.bottom);return '<line class="grid" x1="'+pad.x+'" x2="'+width+'" y1="'+yy+'" y2="'+yy+'"/><text x="0" y="'+(yy+4)+'">'+fmt(max*r)+'</text>'}).join(''),axis=values.map((item,i)=>'<text x="'+x(i)+'" y="'+(height-8)+'" text-anchor="middle">'+esc(item.label)+'</text>').join(''),pointsSvg=values.map((item,i)=>'<circle class="point" cx="'+x(i)+'" cy="'+y(item)+'" r="4"><title>'+esc(item.label)+': '+fmt(item.value)+'</title></circle>').join('');return '<svg viewBox="0 0 '+width+' '+height+'" role="img" aria-label="'+aria+'"><defs><linearGradient id="areaGradient" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stop-color="#9a8cff" stop-opacity=".3"/><stop offset="1" stop-color="#9a8cff" stop-opacity="0"/></linearGradient></defs>'+grid+'<polygon points="'+area+'" fill="url(#areaGradient)"/><polyline class="line" points="'+points+'"/>'+pointsSvg+axis+'</svg>'}function trendValues(){const f=state.filters,grouped=new Map();for(const row of state.model?.timeline||[]){if(f.search&&!([row.tool_name,row.tool_id,row.tool_key,row.slug].some(item=>String(item||'').toLowerCase().includes(f.search))))continue;if(f.domain&&row.domain!==f.domain)continue;if(f.language&&row.language!==f.language)continue;if(f.category&&row.category!==f.category)continue;const date=row.period_start||row.month+'-01';if(f.month&&!date.startsWith(f.month))continue;const d=new Date(date+'T00:00:00Z');let key=date;if(state.granularity==='week'){const day=(d.getUTCDay()+6)%7;d.setUTCDate(d.getUTCDate()-day);key=d.toISOString().slice(0,10)}else if(state.granularity==='month')key=date.slice(0,7);grouped.set(key,(grouped.get(key)||0)+val(row))}return [...grouped].sort((a,b)=>a[0].localeCompare(b[0])).map(([label,value])=>({label,value}))}
function ranking(rows){const active=rows.filter(row=>val(row)>0).sort((a,b)=>val(b)-val(a)).slice(0,10);if(!active.length)return '<div class="empty">—</div>';const max=val(active[0]);return '<div class="list">'+active.map((row,i)=>'<div class="list-row"><div class="rank">'+(i+1)+'</div><div class="list-name">'+esc(row.tool_name)+'<small>'+esc(row.category)+' · '+esc(row.domain)+'</small><div class="bar-wrap"><div class="bar" style="width:'+Math.max(2,val(row)/max*100)+'%"></div></div></div><div class="list-value">'+fmt(val(row))+'</div></div>').join('')+'</div>'}function changes(rows,positive){const list=rows.filter(row=>positive?row.delta>0:row.delta<0).sort((a,b)=>positive?b.delta-a.delta:a.delta-b.delta).slice(0,8);return list.length?list.map(row=>'<div class="change-row"><span>'+esc(row.tool_name)+'</span><span class="'+(positive?'up':'down')+'">'+(row.delta>=0?'+':'')+fmt(row.delta)+' <small>'+pct(row.changePercent)+'</small></span></div>').join(''):'<div class="empty">—</div>'}function statusPill(status){return '<span class="pill '+String(status).replaceAll(' ','-')+'">'+esc(status)+'</span>'}
function renderOverview(){const month=selectedMonth(),rows=monthlyRows(),active=rows.filter(row=>val(row)>0),all=aggregateMonthly(rawFiltered()),trend=state.granularity==='month'?state.model.months.map(m=>({label:m,value:all.filter(row=>row.month===m).reduce((sum,row)=>sum+val(row),0)})):trendValues(),metricPicker='<div class="metric-picker">'+sourceConfig[state.source].metrics.map(metric=>'<button data-metric="'+metric+'" class="'+(state.metric===metric?'active':'')+'">'+metricLabels[metric]+'</button>').join('')+'</div>',granularityPicker='<div class="granularity-picker"><button data-granularity="month" class="'+(state.granularity==='month'?'active':'')+'">Meses</button><button data-granularity="week" class="'+(state.granularity==='week'?'active':'')+'">Semanas</button><button data-granularity="day" class="'+(state.granularity==='day'?'active':'')+'">Días</button></div>',total=active.reduce((sum,row)=>sum+val(row),0);qs('page-overview').innerHTML='<div class="cards">'+card('Tools activas',fmt(new Set(active.map(toolKey)).size),month)+card(metricLabels[state.metric],fmt(total),'total')+card('Nuevas',fmt(active.filter(row=>row.status==='nueva').length),'primera actividad','up')+card('Crecen',fmt(active.filter(row=>row.status==='crece').length),'comparativa mensual','up')+card('Decrecen',fmt(active.filter(row=>row.status==='decrece').length),'comparativa mensual','down')+'</div><div class="layout">'+panel('Evolución','<div class="chart">'+chart(trend,metricLabels[state.metric])+'</div>',metricPicker+granularityPicker)+panel('Top tools',ranking(rows),'<span class="panel-subtitle">'+active.length+'</span>')+'</div><div class="layout">'+panel('Crecimiento',changes(active,true))+panel('Caídas',changes(active,false))+'</div>'}
function renderTools(){const rows=monthlyRows().sort((a,b)=>state.sort==='tool_name'?String(a.tool_name).localeCompare(String(b.tool_name),'es'):state.direction==='asc'?Number(a[state.sort]??a.value)-Number(b[state.sort]??b.value):Number(b[state.sort]??b.value)-Number(a[state.sort]??a.value));const headers=[['tool_name','Tool'],['domain','Dominio'],['language','Idioma'],['category','Categoría'],[state.metric,metricLabels[state.metric]],['previousValue','Anterior'],['delta','Δ'],['status','Estado']];const head=headers.map(([key,label])=>'<th data-sort="'+key+'">'+label+'</th>').join('');const body=rows.slice(0,300).map(row=>'<tr><td>'+esc(row.tool_name)+'<small>'+esc(row.tool_key)+'</small></td><td>'+esc(row.domain)+'</td><td>'+esc(row.language)+'</td><td>'+esc(row.category)+'</td><td class="num">'+fmt(val(row))+'</td><td class="num">'+fmt(row.previousValue)+'</td><td class="num '+(row.delta>=0?'up':'down')+'">'+(row.delta>=0?'+':'')+fmt(row.delta)+'<small>'+pct(row.changePercent)+'</small></td><td>'+statusPill(row.status)+'</td></tr>').join('');qs('page-tools').innerHTML=panel('Tools','<div class="table-tools"><span>'+rows.length+' identidades</span><select id="status"><option value="">Todos</option><option value="nueva">Nuevas</option><option value="crece">Crecen</option><option value="decrece">Decrecen</option><option value="estable">Estables</option><option value="sin datos">Sin datos</option></select></div><div class="table-wrap"><table><thead><tr>'+head+'</tr></thead><tbody>'+body+'</tbody></table></div>');const status=qs('status');status.value=state.filters.status;status.addEventListener('input',()=>{state.filters.status=status.value;render()});qs('page-tools').querySelectorAll('th[data-sort]').forEach(th=>th.addEventListener('click',()=>{const key=th.dataset.sort;if(state.sort===key)state.direction=state.direction==='asc'?'desc':'asc';else{state.sort=key;state.direction='desc'}render()}))}
function renderTrend(){const values=trendValues(),picker='<div class="granularity-picker"><button data-granularity="month" class="'+(state.granularity==='month'?'active':'')+'">Meses</button><button data-granularity="week" class="'+(state.granularity==='week'?'active':'')+'">Semanas</button><button data-granularity="day" class="'+(state.granularity==='day'?'active':'')+'">Días</button></div>',body=values.slice().reverse().slice(0,300).map(item=>'<tr><td>'+esc(item.label)+'</td><td class="num">'+fmt(item.value)+'</td></tr>').join('');qs('page-trend').innerHTML=panel('Tendencia','<div class="chart">'+chart(values,metricLabels[state.metric])+'</div>',picker)+panel('Periodos','<div class="table-wrap"><table><thead><tr><th>Periodo</th><th class="num">'+metricLabels[state.metric]+'</th></tr></thead><tbody>'+body+'</tbody></table></div>')}
function segmentRows(field){const month=selectedMonth(),groups=new Map();for(const row of rawFiltered().filter(row=>row.month===month)){const label=row[field]||'—';let item=groups.get(label);if(!item)item={label,metric:0,tools:new Set()};item.metric+=val(row);if(val(row)>0)item.tools.add(toolKey(row));groups.set(label,item)}return [...groups.values()].sort((a,b)=>b.metric-a.metric)}function renderSegments(){const dims=[['domain','Dominios'],['language','Idiomas'],['category','Categorías']];qs('page-segments').innerHTML='<div class="data-grid">'+dims.map(([field,label])=>'<div class="data-box"><span>'+label+'</span><strong>'+segmentRows(field).length+'</strong></div>').join('')+'</div><div class="layout">'+dims.map(([field,label])=>panel(label,'<div class="table-wrap"><table><thead><tr><th>'+label.slice(0,-1)+'</th><th class="num">Tools</th><th class="num">'+metricLabels[state.metric]+'</th></tr></thead><tbody>'+segmentRows(field).slice(0,100).map(row=>'<tr><td>'+esc(row.label)+'</td><td class="num">'+row.tools.size+'</td><td class="num">'+fmt(row.metric)+'</td></tr>').join('')+'</tbody></table></div>')).join('')+'</div>'}
function renderAlerts(){const rows=monthlyRows().filter(row=>row.status!=='sin datos'),newRows=rows.filter(row=>row.status==='nueva').sort((a,b)=>val(b)-val(a)),upRows=rows.filter(row=>row.delta>0).sort((a,b)=>b.delta-a.delta),downRows=rows.filter(row=>row.delta<0).sort((a,b)=>a.delta-b.delta);const table=(title,list)=>panel(title,'<div class="table-wrap"><table><thead><tr><th>Tool</th><th>'+metricLabels[state.metric]+'</th><th>Anterior</th><th>Δ</th><th>Estado</th></tr></thead><tbody>'+list.slice(0,100).map(row=>'<tr><td>'+esc(row.tool_name)+'<small>'+esc(row.tool_key)+'</small></td><td class="num">'+fmt(val(row))+'</td><td class="num">'+fmt(row.previousValue)+'</td><td class="num '+(row.delta>=0?'up':'down')+'">'+(row.delta>=0?'+':'')+fmt(row.delta)+' <small>'+pct(row.changePercent)+'</small></td><td>'+statusPill(row.status)+'</td></tr>').join('')+'</tbody></table></div>');qs('page-alerts').innerHTML='<div class="cards">'+card('Nuevas',newRows.length)+card('Crecen',upRows.length,'','up')+card('Decrecen',downRows.length,'','down')+card('Con datos',rows.filter(row=>val(row)>0).length)+card('Mes',selectedMonth())+'</div><div class="layout">'+table('Nuevas',newRows)+table('Crecimiento',upRows)+'</div>'+table('Caídas',downRows)}
function renderQuality(){const rows=state.model.rows,timeline=state.model.timeline||[],domains=new Set(rows.map(row=>row.domain)),languages=new Set(rows.map(row=>row.language)),categories=new Set(rows.map(row=>row.category)),periods=new Set(timeline.map(row=>row.period_start));qs('page-quality').innerHTML='<div class="data-grid">'+[['Filas mensuales',rows.length],['Filas diarias',timeline.length],['Dominios',domains.size],['Idiomas',languages.size],['Categorías',categories.size],['Meses',state.model.months.length],['Último mes',state.model.latestMonth],['Fuente',state.model.sourceAvailable?'OK':'—']].map(([label,value])=>'<div class="data-box"><span>'+label+'</span><strong>'+esc(value)+'</strong></div>').join('')+'</div>'+panel('Cobertura','<div class="table-wrap"><table><thead><tr><th>Periodo</th><th class="num">Filas</th><th class="num">'+metricLabels[state.metric]+'</th></tr></thead><tbody>'+state.model.months.map(month=>{const period=rows.filter(row=>row.month===month);return '<tr><td>'+month+'</td><td class="num">'+period.length+'</td><td class="num">'+fmt(period.reduce((sum,row)=>sum+val(row),0))+'</td></tr>'}).join('')+'</tbody></table></div>')+panel('Estado','<div class="data-grid"><div class="data-box"><span>Fuente</span><strong>'+esc(sourceConfig[state.source].title)+'</strong></div><div class="data-box"><span>Periodos diarios</span><strong>'+periods.size+'</strong></div><div class="data-box"><span>Errores</span><strong>'+((qs('error').textContent||'').length?'1':'0')+'</strong></div></div>')}
function render(){if(!state.model)return;document.querySelectorAll('.page').forEach(page=>page.classList.toggle('active',page.id==='page-'+state.page));document.querySelectorAll('.nav-item').forEach(item=>item.classList.toggle('active',item.dataset.page===state.page));qs('page-title').textContent={overview:'Resumen',tools:'Tools',trend:'Tendencia',segments:'Segmentos',alerts:'Alertas',quality:'Calidad'}[state.page];if(state.page==='overview')renderOverview();if(state.page==='tools')renderTools();if(state.page==='trend')renderTrend();if(state.page==='segments')renderSegments();if(state.page==='alerts')renderAlerts();if(state.page==='quality')renderQuality();bindDynamic()}function bindDynamic(){document.querySelectorAll('[data-metric]').forEach(button=>button.addEventListener('click',()=>{state.metric=button.dataset.metric;render()}));document.querySelectorAll('[data-granularity]').forEach(button=>button.addEventListener('click',()=>{state.granularity=button.dataset.granularity;render()}))}function setOptions(id,values,first){const select=qs(id),current=select.value;select.innerHTML='<option value="">'+first+'</option>'+[...new Set(values)].filter(Boolean).sort((a,b)=>String(a).localeCompare(String(b),'es')).map(value=>'<option value="'+esc(value)+'">'+esc(value)+'</option>').join('');if([...select.options].some(option=>option.value===current))select.value=current}function updateControls(){setOptions('month',state.model.months,'Último');setOptions('domain',state.model.rows.map(row=>row.domain),'Todos');setOptions('language',state.model.rows.map(row=>row.language),'Todos');setOptions('category',state.model.rows.map(row=>row.category),'Todas');qs('month').value=state.filters.month;qs('domain').value=state.filters.domain;qs('language').value=state.filters.language;qs('category').value=state.filters.category;qs('source-title').textContent=sourceConfig[state.source].title;document.querySelectorAll('[data-source]').forEach(button=>button.classList.toggle('active',button.dataset.source===state.source))}async function load(force=false){qs('refresh').disabled=true;try{const response=await fetch('/api/data?source='+encodeURIComponent(state.source)+'&metric='+state.metric+(force?'&refresh=1':''),{cache:'no-store'});const payload=await response.json();if(!payload.ok)throw new Error(payload.error);state.model=payload.model;updateControls();qs('last-updated').textContent=payload.meta?.lastSync?new Date(payload.meta.lastSync).toLocaleTimeString('es-ES',{hour:'2-digit',minute:'2-digit'}):'—';qs('source-info').textContent=state.model.rows.length.toLocaleString('es-ES')+' filas · '+state.model.months.length+' meses · '+(state.model.timeline?.length||0).toLocaleString('es-ES')+' periodos';if(payload.meta?.lastSyncError){qs('error').textContent=payload.meta.lastSyncError;qs('error').classList.remove('hidden')}else qs('error').classList.add('hidden');render()}catch(error){qs('error').textContent=error.message;qs('error').classList.remove('hidden')}finally{qs('refresh').disabled=false}}function exportCsv(){const rows=state.page==='tools'?monthlyRows():state.model.rows.filter(row=>row.month===selectedMonth());const headers=['month','tool_key','tool_name','domain','language','category',state.metric,'previousValue','delta','status'];const csv=[headers.join(','),...rows.map(row=>headers.map(field=>String(row[field]??'').replaceAll('"','""')).map(value=>value.includes(',')?'"'+value+'"':value).join(','))].join('\n');const link=document.createElement('a');link.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));link.download='tools-pulse-'+state.source+'-'+state.page+'.csv';link.click();setTimeout(()=>URL.revokeObjectURL(link.href),1000)}document.querySelectorAll('.nav-item').forEach(item=>item.addEventListener('click',()=>{state.page=item.dataset.page;render()}));document.querySelectorAll('[data-source]').forEach(button=>button.addEventListener('click',()=>{state.source=button.dataset.source;state.metric=sourceConfig[state.source].metrics[0];state.filters.month='';state.filters.domain='';state.filters.language='';state.filters.category='';state.filters.status='';load()}));['search','month','domain','language','category'].forEach(id=>qs(id).addEventListener('input',()=>{state.filters[id]=qs(id).value.toLowerCase();render()}));qs('clear').addEventListener('click',()=>{state.filters={search:'',month:'',domain:'',language:'',category:'',status:''};['search','month','domain','language','category'].forEach(id=>qs(id).value='');render()});qs('refresh').addEventListener('click',()=>load(true));qs('export').addEventListener('click',exportCsv);load();setInterval(()=>load(),300000);
state.sort=state.metric;
document.querySelectorAll('[data-source]').forEach(button=>button.addEventListener('click',()=>{state.sort=sourceConfig[button.dataset.source].metrics[0];state.direction='desc'}));
['search','month','domain','language','category'].forEach(id=>qs(id).addEventListener('input',()=>{state.filters[id]=id==='search'?qs(id).value.toLowerCase():qs(id).value;render()}));
</script></body></html>`;
    const fixed = html
        .replace("join('" + String.fromCharCode(10) + "')", "join('\\n')")
        .replace('null})})}', 'null}})}')
        .replace('.chart{height:280px;', '.chart{height:370px;')
        .replace('</style>', `<style>
.chart{min-height:330px}.chart svg{overflow:visible;touch-action:none}.chart-hitbox{fill:transparent;cursor:crosshair}.chart-hitbox:hover{fill:#9a8cff12}.chart-hover-line{stroke:#a89cff;stroke-width:1;stroke-dasharray:4 5;opacity:0;pointer-events:none}.chart-hover-dot{fill:#11182a;stroke:#fff;stroke-width:2.5;opacity:0;pointer-events:none}.chart-tooltip{position:absolute;z-index:4;min-width:116px;max-width:210px;padding:8px 10px;border:1px solid #46527a;border-radius:9px;background:#11182af2;box-shadow:0 9px 24px #0008;color:#fff;font:11px Inter,system-ui,sans-serif;pointer-events:none;opacity:0;transform:translate(-50%,-100%);transition:opacity .12s ease;white-space:nowrap}.chart-tooltip strong{display:block;font-size:13px;margin-top:3px}.chart-tooltip.visible{opacity:1}
</style>`)
        .replace('</body>', `<script>
(()=>{const ns='http://www.w3.org/2000/svg';const decorate=svg=>{if(svg.dataset.hitboxReady)return;const points=[...svg.querySelectorAll('circle.point')];if(!points.length)return;svg.dataset.hitboxReady='1';const box=svg.viewBox.baseVal,height=box.height||260,xs=points.map(point=>Number(point.getAttribute('cx'))),wrapper=svg.parentElement;const tooltip=document.createElement('div');tooltip.className='chart-tooltip';wrapper.appendChild(tooltip);const guide=document.createElementNS(ns,'line');guide.setAttribute('class','chart-hover-line');guide.setAttribute('y1','10');guide.setAttribute('y2',String(height-36));const dot=document.createElementNS(ns,'circle');dot.setAttribute('class','chart-hover-dot');dot.setAttribute('r','5');svg.querySelector('g.chart-overlay')?.remove();const overlay=document.createElementNS(ns,'g');overlay.setAttribute('class','chart-overlay');overlay.append(guide,dot);svg.appendChild(overlay);const edge=i=>i===0?0:(xs[i-1]+xs[i])/2;const nextEdge=i=>i===xs.length-1?box.width:(xs[i]+xs[i+1])/2;points.forEach((point,i)=>{const hitbox=document.createElementNS(ns,'rect');hitbox.setAttribute('class','chart-hitbox');hitbox.setAttribute('x',String(edge(i)));hitbox.setAttribute('y','0');hitbox.setAttribute('width',String(Math.max(18,nextEdge(i)-edge(i))));hitbox.setAttribute('height',String(height));const title=point.querySelector('title')?.textContent||'';hitbox.setAttribute('aria-label',title);hitbox.setAttribute('tabindex','0');const show=()=>{const x=xs[i],y=Number(point.getAttribute('cy'));guide.setAttribute('x1',String(x));guide.setAttribute('x2',String(x));guide.style.opacity='1';dot.setAttribute('cx',String(x));dot.setAttribute('cy',String(y));dot.style.opacity='1';tooltip.textContent='';const label=document.createElement('span');label.textContent=title.split(':')[0]||'Periodo';const value=document.createElement('strong');value.textContent=title.includes(':')?title.slice(title.indexOf(':')+1).trim():'';tooltip.append(label,value);const r=hitbox.getBoundingClientRect(),w=wrapper.getBoundingClientRect();tooltip.style.left=(r.left+r.width/2-w.left)+'px';tooltip.style.top=Math.max(12,r.top-w.top+8)+'px';tooltip.classList.add('visible')};const hide=()=>{guide.style.opacity='0';dot.style.opacity='0';tooltip.classList.remove('visible')};hitbox.addEventListener('pointerenter',show);hitbox.addEventListener('focus',show);hitbox.addEventListener('pointerleave',hide);hitbox.addEventListener('blur',hide);overlay.appendChild(hitbox)})};const scan=()=>document.querySelectorAll('.chart svg').forEach(decorate);new MutationObserver(scan).observe(document.body,{childList:true,subtree:true});scan()})();
</script></body>`);
    return fixed;
}
